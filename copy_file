class User(AbstractUser):
    access_level = models.TextField(null=True, blank=True)
give the views and serializer and urls for create and update api's. use modelserializer and create and update function inbuilt. and i need few fields like username, email, is_active and access_level. none other not needed so add extra_kwargs for not required of fields like password, last_login
