const bomScope  = button.data('scope');
            const bomMethod = button.data('method');
            const bomProcessType = document.getElementById("process_type")

            if (bomScope === "BOSF" && bomMethod === "CSC"){
                
            }
<td><input id="process_type" value="${proc.process_type || ''}" readonly style="background-color:e6e6e6"></td>
 in if condition chnage process type input to select with options Inhouse, outsourced. default value is inhouse
process_type is multiple so do it every row same
