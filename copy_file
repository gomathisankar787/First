import Bom from "../Bom/bom.tsx";
<button
className="action-btn action-view"
data-tooltip="Bom" onClick={Bom}>
{/* onClick={() => console.log("View", item.cscId)} */}
<svg viewBox="0 0 24 24" fill="none">
  <path
    d="M2 12s4-7 10-7 10 7 10 7-4 7-10 7S2 12 2 12z"
    stroke="currentColor"
    strokeWidth="2"
  />
  <circle cx="12" cy="12" r="3" stroke="currentColor" strokeWidth="2" />
</svg>
</button>

give the bom tsx and css code it should contain item.cscId one card that contains details CSC UNIQUE ID, PART NUMBER, REVISION, DESCRIPTION, ESTIMATED BY, RM BASE, PROCESS BASE, OH PROFIT BASE  
below left side have three tabs bom, bom summary, graph view right side Total estimated cost variable and value like Total Estimated Cost: 97.68 and below bom list table in first default one row should exist all table cell are inputs
so direct save variables are s.no, level, part no, rev, parts description, qty, process type(select), scope(select), method(select), is nesting needed(selection), action (add row, edit, delete)icons with pagination and in upper title bom list in left side and right side two buttons save bom data and update bom data for bulk creation and bulk updation
