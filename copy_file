const isNestingNeeded = button.data('is_nesting_needed')
            console.log('isdf',isNestingNeeded)
            const noOfBlanksInput = document.querySelector('input[name="no_of_blanks"]');

            if (isNestingNeeded === "Yes") {
                // console.log('if worked')
                // noOfBlanksInput.removeAttribute('readonly',false);
                console.log('noofblanks',noOfBlanksInput)                
                noOfBlanksInput.readOnly = false;           // preferred over set/removeAttribute
                noOfBlanksInput.classList.remove('readonly');


            } else {
                // noOfBlanksInput.setAttribute('readonly',true);
                // document.querySelector('input[name="no_of_blanks"]').style.backgroundColor= "#e9ecef";
                                
                noOfBlanksInput.readOnly = true;
                noOfBlanksInput.classList.add('readonly');
                noOfBlanksInput.style.backgroundColor = "#e9ecef";
                console.log('noofblanks',noOfBlanksInput)

            }


isdf No
34/:7591 noofblanks <input type name=​"no_of_blanks" value=​"280" id=​"no_of_blanks" readonly class=​"readonly">​
but didn't affect the ui
