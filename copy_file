<div id="pdfContent" style="padding:20px; background:white;">

                <div id="cscCardDetails"></div>
                <br>

                <h2>BOM LIST</h2>
                <div id="pdfBomTable"></div>
                <br><br>

                <h2>BOM SUMMARY</h2>
                <div id="pdfBomSummary"></div>
                <br><br>

                <h2>GRAPH VIEW</h2>
                <canvas id="pdfChartCanvas"></canvas>

                <br><br>

                <h2>SM MATERIAL / PROCESS / OH PROFIT DETAILS</h2>
                <div id="pdfModalData"></div>

            </div>

async function downloadBOMPDF() {

                const { jsPDF } = window.jspdf;
                const pdf = new jsPDF('p', 'mm', 'a4');

                // ✅ Force graph tab to render
                document.getElementById("graphview-tab").click();

                // wait 800ms safely
                await new Promise(resolve => setTimeout(resolve, 800));

                // Clone Card Details
                document.getElementById("cscCardDetails").innerHTML =
                    document.querySelector("#card_details .card-body").outerHTML;

                // Clone BOM table
                document.getElementById("pdfBomTable").innerHTML =
                    document.querySelector("#pills-home .custom-table").outerHTML;

                // Clone BOM summary
                document.getElementById("pdfBomSummary").innerHTML =
                    document.querySelector("#pills-bomsummary .custom-table").outerHTML;

                // Clone Modal Details
                document.getElementById("pdfModalData").innerHTML =
                    document.querySelector("#updatecscModal .modal-body").outerHTML;

                // Capture chart safely
                const originalChartCanvas = document.querySelector("#bomFloatingBarH");

                if (originalChartCanvas && originalChartCanvas.width > 0) {

                    const chartImage = originalChartCanvas.toDataURL("image/png");

                    const img = document.createElement("img");
                    img.src = chartImage;
                    img.style.width = "100%";

                    const chartContainer = document.getElementById("pdfChartCanvas");
                    chartContainer.replaceWith(img);
                }

                const element = document.getElementById("pdfContent");

                const canvas = await html2canvas(element, { scale: 2 });

                const imgData = canvas.toDataURL("image/png");

                const imgWidth = 210;
                const pageHeight = 295;
                const imgHeight = canvas.height * imgWidth / canvas.width;

                let heightLeft = imgHeight;
                let position = 0;

                pdf.addImage(imgData, 'PNG', 0, position, imgWidth, imgHeight);
                heightLeft -= pageHeight;

                while (heightLeft > 0) {
                    position = heightLeft - imgHeight;
                    pdf.addPage();
                    pdf.addImage(imgData, 'PNG', 0, position, imgWidth, imgHeight);
                    heightLeft -= pageHeight;
                }

                pdf.save("BOM_Report.pdf");
            }

if i click download time of process on rendering the actual screen component is breaking. modal values didn't show i need the individual bom modal components with their corresponded values.
